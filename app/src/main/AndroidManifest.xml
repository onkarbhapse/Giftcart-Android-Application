<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.login">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".show_productActivity"></activity>
        <activity android:name=".AdminUserProductsActivity" />
        <activity android:name=".AdminNewOrderActivity" />
        <activity android:name=".ThankYouActivity" />
        <activity android:name=".conformOrderActivity" />
        <activity android:name=".Product_descriptinActivity" />
        <activity
            android:name=".Homepage"
            android:label="@string/title_activity_homepage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AdmincategoryActivity" />
        <activity android:name=".AdminaddNewproductActivity" />
        <activity android:name=".Registration" />
        <activity android:name=".MainActivity" />
        <activity android:name=".splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>